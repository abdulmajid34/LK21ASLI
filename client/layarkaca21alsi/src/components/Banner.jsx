import React from 'react'
import { useSelector } from 'react-redux';


function Banner() {
    // const now_playing = useSelector(state => state.movie_now_playing);
    // console.log(now_playing, 'log');
    

    return (
        <div className='mt-0'>
            
          
        </div>
    )
}

export default Banner